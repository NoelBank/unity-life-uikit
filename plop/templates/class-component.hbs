import React from 'react';
import { {{name}}Props, {{name}}State } from './interfaces';

export default class {{name}} extends React.Component<{{name}}Props, {{name}}State> {
  constructor(props: {{name}}Props) {
    super(props);
  }

  render(): JSX.Element {
    return (
      <div>{this.props.children}</div>
    );
  }
}

